# Installing i3
$ sudo apt install xinit
$ sudo apt install i3
$ sudo apt-get install zsh 

# Setting up Git
$ sudo apt-get install git
$ sudo apt-get install firefox
$ sudo apt-get install xclip
$ ssh-keygen -t ed25519 "a.elhag01@gmail.com"
$ eval "$(ssh-agent -s)" 
$ cat ~/.ssh/id_ed25519.pub | xclip -sel clip

# Syncing
$ git clone git@github.com:a-elhag/dotfiles.git ~/.dotfiles
$ chsh -s /usr/bin/zsh
$ cd ~/.dotfiles/setup
